# Secrets — never commit real API keys or config.
.env
.env.*.local

# Dependencies
node_modules/

# Runtime logs, including rotated generations (Step 104: system.log.1,
# system.log.2, ...).
logs/*.log
logs/*.log.*

# Persisted intelligence run records (Step 102) — runtime-generated,
# never source.
data/runs.jsonl

# OS/editor cruft.
.DS_Store
Thumbs.db
